// use formatter::checks::identical_record_entries;

#[cfg(test)]
mod tests {
    // use super::*;

    #[test]
    fn it_works() {
        // let result = identical_record_entries(2, 2);
        assert_eq!(4, 4);
    }
}
